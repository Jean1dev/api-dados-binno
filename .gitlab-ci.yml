image: node:latest

stages:
  - build
  - db_migrate
  - deploy_prod

cache:
  paths:
    - node_modules/

install_dependencies:
  stage: build
  script:
    - npm install
  artifacts:
    paths:
      - node_modules/

configure_databse:
  stage: db_migrate
  only:
    - master
  script:
    - export HOST_DB=$HOST_DB
    - export USERNAME_DB=$USERNAME_DB
    - export PASSWD_DB=$PASSWD_DB
    - export DB_NAME=$DB_NAME
    - export SSL_CONNECTION=true
    - npx sequelize-cli db:migrate

test_environment:
  stage: deploy_prod
  image: ruby:latest
  when: manual
  only:
    - master
  script:
    - apt-get update -qy
    - apt-get install -y ruby-dev
    - gem install dpl
    - dpl --provider=heroku --app=api-dados-binno --api-key=$HEROKU_API_KEY
